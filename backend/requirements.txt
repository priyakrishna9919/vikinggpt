# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Environment variables
python-dotenv==1.0.1

# Web scraping — Playwright for JS-rendered CSU pages
playwright==1.44.0

# Local embeddings — free, runs on your machine, no API key
sentence-transformers==3.0.1

# Vector database — updated version compatible with numpy 2.0
chromadb==0.4.24

# Pin numpy to 1.x to avoid the np.float_ removal issue
numpy==1.26.4

# HTTP client — used to call Groq API
httpx==0.27.0

# Data validation
pydantic==2.7.2

# Scheduling
apscheduler==3.10.4

# Logging
loguru==0.7.2
