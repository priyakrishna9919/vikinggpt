# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Environment variables
python-dotenv==1.0.1

# Web scraping — Playwright for JS-rendered CSU pages
playwright==1.44.0

# Local embeddings — free, runs on your machine, no API key
sentence-transformers==3.0.1

# Vector database — ChromaDB stores embeddings on disk
chromadb==0.5.0

# HTTP client — used to call Groq API
httpx==0.27.0

# Data validation
pydantic==2.7.2

# Scheduling — re-scrape CSU pages periodically
apscheduler==3.10.4

# Logging
loguru==0.7.2

# NOTE: No OpenAI, no Anthropic needed!
# LLM  → Groq free tier (Llama 3.3 70B) — sign up at console.groq.com
# Embed → sentence-transformers (local, completely free)
