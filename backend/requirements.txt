# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Environment variables
python-dotenv==1.0.1

# Web scraping — Playwright for JS-rendered CSU pages
playwright==1.44.0

# Local embeddings — runs on your machine, no API key needed
# Downloads all-MiniLM-L6-v2 (~90MB) automatically on first run
sentence-transformers==3.0.1

# Vector database — ChromaDB stores and retrieves embeddings
chromadb==0.5.0

# Anthropic Claude SDK — powers chat responses
anthropic==0.28.0

# HTTP client
httpx==0.27.0

# Data validation
pydantic==2.7.2

# Scheduling — re-scrape CSU pages on a cron schedule
apscheduler==3.10.4

# Logging
loguru==0.7.2
