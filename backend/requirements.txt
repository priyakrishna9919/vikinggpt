# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Environment variables
python-dotenv==1.0.1

# Web scraping — requests + BeautifulSoup (replaces Playwright)
# Simpler, no browser needed, works great on Windows
requests==2.31.0
beautifulsoup4==4.12.3

# Local embeddings — free, runs on your machine, no API key
sentence-transformers==3.0.1

# Vector database — compatible with numpy 1.x
chromadb==0.4.24

# Pin numpy to avoid np.float_ removal error in numpy 2.0
numpy==1.26.4

# HTTP client — used to call Groq API
httpx==0.27.0

# Data validation
pydantic==2.7.2

# Scheduling
apscheduler==3.10.4

# Logging
loguru==0.7.2
